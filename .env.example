# Database — PostgreSQL connection string
# Required for backend and Prisma migrations
DATABASE_URL=postgresql://user:password@localhost:5432/kasa

# Test database — separate instance for integration tests (CI only)
# DATABASE_URL_TEST=postgresql://user:password@localhost:5432/kasa_test

# HTTP server port (default: 3000)
# PORT=3000

# Environment (development | production | test)
# NODE_ENV=development

# CORS — allowed frontend origin (default: http://localhost:5173)
# CORS_ORIGIN=http://localhost:5173

# Frontend — backend API base URL (Vite public env var, prefix VITE_)
# VITE_API_URL=http://localhost:3000
